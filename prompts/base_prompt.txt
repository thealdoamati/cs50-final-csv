You are a senior Python developer.

Your task is to generate a complete, production-ready Python script.

--------------------------------------------------
CONTEXT
--------------------------------------------------

A CSV file named "csv_file.csv" exists inside the folder "input_csv".

The CSV header columns are:
{header}

Example rows from the CSV:
{examples}

--------------------------------------------------
TASK
--------------------------------------------------

Based strictly on this CSV structure, create a Python script that performs the following action:

{action}

--------------------------------------------------
GENERAL RULES
--------------------------------------------------

1. The script must:
   - Read the CSV file from "input_csv/csv_file.csv".
   - Dynamically handle the provided column structure.
   - Implement exactly what is described in the action.
   - Place all generated outputs inside the folder "output_files".

2. The script must adapt its output format according to the requested action:
   - If the action involves exporting a spreadsheet or table file, generate BOTH:
        • A CSV file
        • An XLSX file
   - If the action specifies another format (e.g., JSON, TXT, etc.), export accordingly.
   - Do NOT generate unnecessary formats that were not requested.

3. If the folder "output_files" does not exist, the script must create it automatically.

4. The script must include proper error handling for:
   - Missing input file
   - Invalid CSV format
   - Missing required columns referenced in the action

5. Use:
   - pandas for data handling
   - openpyxl only if XLSX export is required

--------------------------------------------------
USAGE INSTRUCTIONS
--------------------------------------------------

At the top of the script, include a clear multiline comment explaining:

- Required Python version
- Required libraries
- How to install dependencies (pip install ...)
- Expected folder structure
- How to run the script from terminal

--------------------------------------------------
TECHNICAL CONSTRAINTS
--------------------------------------------------

- The script must be runnable as a standalone file.
- Include all necessary imports.
- Use `if __name__ == "__main__":`
- Do not include explanations outside the code.
- Output only valid Python code.

Return only the final Python script.